# รายละเอียดการแก้ไข (Modifications)

การแก้ไขนี้มีจุดประสงค์เพื่อให้ AssetStudioGUI สามารถใช้งานร่วมกับ DLL ที่ได้จากการ dump (เช่น Il2CppDumper) ได้สะดวกยิ่งขึ้น โดยเฉพาะการอ่านข้อมูลใน `MonoBehaviour`

### รายการแก้ไขหลัก:

#### 1. ระบบตรวจจับโฟลเดอร์ Assembly อัตโนมัติ (Auto-detection)
- เพิ่มฟังก์ชัน `AutoDetectAssemblyFolder` ใน `Studio.cs`
- เมื่อมีการโหลดไฟล์หรือโฟลเดอร์ โปรแกรมจะค้นหาโฟลเดอร์ DLL ต่อไปนี้โดยอัตโนมัติ:
    - `Managed`
    - `DummyDll`
    - `dump`
    - `cpp/DummyDll` (สำหรับ Il2CppDumper บางเวอร์ชัน)
- หากตรวจพบ จะทำการโหลด DLL เข้าสู่ระบบทันทีเพื่อใช้ในการถอดรหัส (Parse) ข้อมูล MonoBehaviour

#### 2. เมนูเลือกโฟลเดอร์ Assembly ด้วยตนเอง
- เพิ่มเมนู **"Specify Unity assemblies folder"** ภายใต้เมนู **Options** ในหน้าหลักของโปรแกรม
- ผู้ใช้สามารถเลือกโฟลเดอร์ที่มี DLL (เช่น `dummy.dll` จากโฟลเดอร์ dump) ได้ทุกเมื่อ โดยไม่จำเป็นต้องรอให้โปรแกรมถาม

#### 3. ปรับปรุงระบบการโหลด MonoBehaviour
- แก้ไข `MonoBehaviourToTypeTree` ใน `Studio.cs` ให้ตรวจสอบสถานะการโหลดของ `AssemblyLoader` อย่างเข้มงวดขึ้น
- หากยังไม่มีการโหลด DLL และระบบตรวจจับอัตโนมัติไม่ทำงาน โปรแกรมจะเปิดหน้าต่างให้ผู้ใช้เลือกโฟลเดอร์ก่อนเริ่มทำการถอดรหัสข้อมูล

#### 4. การจัดการหน่วยความจำและข้อมูลเก่า
- ปรับปรุงฟังก์ชัน `ResetForm` ให้ทำการล้างข้อมูลใน `AssemblyLoader` ทุกครั้งที่มีการโหลดไฟล์ใหม่ เพื่อป้องกันการใช้ข้อมูล DLL ข้ามเกมหรือข้ามเวอร์ชัน

---
*แก้ไขโดย Pi Coding Assistant - 2026-05-29*
